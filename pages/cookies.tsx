import {useEffect,useState} from 'react';import Institutional from '../components/Institutional';export default function Cookies(){const [v,setV]=useState<string|null>(null);useEffect(()=>setV(localStorage.getItem('brdb-cookie')),[]);const save=(x:string|null)=>{if(x)localStorage.setItem('brdb-cookie',x);else localStorage.removeItem('brdb-cookie');setV(x)};return <Institutional title="Cookies" desc="Controle de preferências locais desta aplicação."><section className="card"><p>Status atual: <b>{v==='accepted'?'Aceitos':v==='rejected'?'Recusados':'Não definido'}</b></p><button className="btn primary" onClick={()=>save('accepted')}>Aceitar</button> <button className="btn" onClick={()=>save('rejected')}>Recusar opcionais</button> <button className="btn" onClick={()=>save(null)}>Redefinir</button></section><section className="section"><h2>Preferências locais</h2><p className="desc">A aplicação utiliza armazenamento local exclusivamente para lembrar sua preferência de consentimento.</p></section></Institutional>}
